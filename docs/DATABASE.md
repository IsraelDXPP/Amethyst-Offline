```markdown
# Database Documentation

## Overview

Amethyst-Offline does not utilize a relational or NoSQL database. No database files, schemas, ORM configurations (e.g., Room for Android, CoreData for iOS, SQLite, or similar), migration scripts, or seeding mechanisms are present in the provided codebase analysis (README.md and referenced build workflows). Data persistence, if any, is likely handled via platform-native storage such as SharedPreferences (Android), UserDefaults/plist files (iOS), or simple file-based storage typical for offline Minecraft launchers based on PojavLauncher.

This document is skipped as no database exists.
```